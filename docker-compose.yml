version: '3.6'

services:
  orden:
    build: 
      context: orden
  plan-ruta:
    build: 
      context: plan-ruta
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - orden
      - plan-ruta
    ports:
      - "8080:8080"


